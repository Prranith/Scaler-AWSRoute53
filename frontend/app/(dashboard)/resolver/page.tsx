import { ComingSoon } from "@/components/shared/ComingSoon";
export default function ResolverPage() {
  return <ComingSoon feature="Resolver" description="Configure DNS resolution for hybrid environments. Create resolver endpoints, rules, and query logs for your VPCs." />;
}
